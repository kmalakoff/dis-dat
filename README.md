## dis-dat

"Run commands in parallel (dad / dis-and-dat) or sequentially (dtd / dis-then-dat)

```
# one then the other
$ dtd "npm install" "npm test"

# all in parallel
$ dad "npm test" "eslint ."
```
